name: scrabble
description: Scrabble helper library.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  archive: ^2.0.13
  build: ^1.6.0

dev_dependencies:
  build_runner: ^1.10.11
  build_web_compilers: ^2.16.0
  pedantic: ^1.9.0
  test: ^1.14.4

executables:
  ? scrabble
