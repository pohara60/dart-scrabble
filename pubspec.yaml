name: scrabble
description: An API and command line tool for finding and scoring legal Scrabble words
version: 0.1.1
repository: https://github.com/pohara60/dart-scrabble

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  archive: ^2.0.13
  args: ^1.6.0
  build: ^1.6.0

dev_dependencies:
  build_runner: ^1.10.11
  build_web_compilers: ^2.16.0
  pedantic: ^1.9.0
  test: ^1.14.4

executables:
  ? scrabble
